

pub struct ${model.name}Meta {
pub mut:
	contentkey	string
	changed bool 						[skip]
	//last used from memory, this to let cache work
	access_last	int						[skip]
	table 	&Table${name.capitalize()} 	[skip]
}