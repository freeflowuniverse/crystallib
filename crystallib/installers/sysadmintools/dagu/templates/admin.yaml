host: "::"                          # default: 127.0.0.1
port: 3333

# path to the DAGs directory
dags: ${homedir}/dags

# Web UI Color & Title
# navbarColor: <ui header color>                               # header color for web UI (e.g. "#ff0000")
navbarTitle: ${args.title}                                 # header title for web UI (e.g. "PROD")

isBasicAuth: true
basicAuthUsername: admin
basicAuthPassword: {DAGU.PASSWD}

isAuthToken: true                                    # enables API token
authToken: {DAGU.AUTHTOKEN}

# Base Config
baseConfig: ${homedir}/config.yaml

# Working Directory
# workDir: ${homedir}/working                        # default: DAG location

# SSL Configuration
# tls:
#     certFile: <path to SSL certificate file>
#     keyFile: <path to SSL key file>
